#include "bits/stdc++.h"
using namespace std;
#define ll long long
#define in cin >>
#define out cout <<
#define for(n) for(ll i=0;i<n;i++)
#define pb push_back
 
 
int main(){
    ll n;in n;
    vector<ll>arr;
    for(n){
        ll temp; in temp;
        arr.pb(temp);
    }
 
    long double sum=0;
    for(n){
        sum= sum+arr[i];
    }
    long double a = sum/n;
    printf("%0.12Lf",a);
    // out setprecision(14)<<a<<endl;
 
}